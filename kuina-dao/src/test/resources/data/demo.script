CREATE TABLE Address(id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY, street VARCHAR, city VARCHAR, state VARCHAR, zip VARCHAR)
CREATE TABLE Department(id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY, name VARCHAR)
CREATE TABLE Employee(id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY, name VARCHAR, salary INTEGER, manager INTEGER, department_id INTEGER, address_id INTEGER, FOREIGN KEY (department_id) REFERENCES Department (id), FOREIGN KEY (address_id) REFERENCES Address (id))
CREATE TABLE Phone(id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY, number VARCHAR, type VARCHAR, employee_id INTEGER, FOREIGN KEY (employee_id) REFERENCES Employee (id))
CREATE TABLE Project(id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY, dtype VARCHAR, name VARCHAR)
CREATE TABLE DesignProject(id INTEGER NOT NULL PRIMARY KEY, FOREIGN KEY (id) REFERENCES Project (id))
CREATE TABLE QualityProject(id INTEGER NOT NULL PRIMARY KEY, FOREIGN KEY (id) REFERENCES Project (id))
CREATE TABLE Employee_Project(employee_id INTEGER NOT NULL, department_id INTEGER NOT NULL, PRIMARY KEY (employee_id, department_id), FOREIGN KEY (employee_id) REFERENCES Employee (id), FOREIGN KEY (department_id) REFERENCES Department (id))

CREATE TABLE DUAL(DUMMY VARCHAR(1))
CREATE USER SA PASSWORD "" ADMIN
SET WRITE_DELAY 60

INSERT INTO Department VALUES (1, 'Tiffany')

INSERT INTO Address VALUES (1, 'Fifth Avenue at 57th Street', 'New York', 'New York', '10022')
INSERT INTO Address VALUES (2, '1980 Northern Boulevard', 'Manhasset', 'New York', '11030')
INSERT INTO Address VALUES (3, '210 North Rodeo Drive', 'Beverly Hills', 'California', '90210')

INSERT INTO Employee VALUES (1, 'NEW YORK', 5000000, NULL, 1, 1)
INSERT INTO Employee VALUES (2, 'AMERICANA MANHASSET', 4000000, 1, 1, 2)
INSERT INTO Employee VALUES (3, 'BEVERLY HILLS', 3000000, 1, 1, 3)

INSERT INTO Phone VALUES (1, '212-755-8000', 'P', 1)
INSERT INTO Phone VALUES (2, '516-869-0800', 'P', 2)
INSERT INTO Phone VALUES (3, '310-273-8880', 'P', 3)
